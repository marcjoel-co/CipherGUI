# Compiled files
*.o
cipher_gui  # Or whatever your executable is named in the Makefile

# macOS specific
.DS_Store

# VS Code specific (if you use it across machines and don't want to share settings)
# .vscode/

# Build artifacts from other systems (if you ever use them)
build/
cmake-build-debug/
cmake-build-release/

# ImGui user settings (often not shared)
imgui.ini