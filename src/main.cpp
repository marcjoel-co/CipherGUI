// src/main.cpp
#include "jay_gui.h" // For run_gui_application()

// This is the ONLY main function in your entire project.
// Its only job is to start the GUI application.
int main(int argc, char** argv) {
    return run_gui_application();
}